---
created: "2026-04-27T17:15:00Z"
last_edited: "2026-04-27T17:15:00Z"
---
# Implementation Tracking: console-ui-docs-and-observability

Build site: context/plans/build-site.md

| Task | Status | Notes |
|------|--------|-------|
| T-066 | DONE | OTel spans `oidc.dcr.{register,read,update,delete,iat.consume}` wired at handler entries via `tracing.NewNamedSpan`. No span attributes added (R7 AC6 satisfied structurally — zero attributes can't carry secrets). 6 tests in `dcr_otel_spans_test.go` pin all 6 R7 ACs through in-memory `tracetest.SpanRecorder` registered as global TracerProvider in `init()` (sync.OnceValue cache means the swap MUST land before first NewNamedSpan call). Also updated the F-200 source-string assertion in `dispatcher_test.go` to match `deps.ConsumeIAT(consumeCtx, regCtx)` (span-scoped child context). Files touched: wire.go, manage_get.go, manage_put.go, manage_delete.go, dcr_otel_spans_test.go (new), dispatcher_test.go (assertion update). |
| T-067 | DONE | OTel metrics under dotted `zitadel.dcr.*` style (R8 AC1-6). New `metrics.go` file: 5 metric name constants + 5 record helpers (best-effort, swallow errors). `init()` registers all 5 metrics on the global meter. Wiring in dispatcher: `metricsResponseWriter` captures status; deferred `recordRegistration` + `recordRequestDuration` on every POST exit; `recordIATConsumed`/`recordIATExhausted` around the consume call site (exhausted detection via `errorMessageContains` matching `Errors.DCR.IAT.Exhausted` i18n key in wrapped zerror); `recordError` in writeDispatchError + writeAuthError + F-219 short-circuit. GET/PUT/DELETE handlers also wired with duration timer. AC6 pin: 5 banned `dcr_*_total` underscore names asserted-NotFound + every `Metric*` constant tested for `zitadel.dcr.` prefix. 12 tests in `dcr_otel_metrics_test.go` pinning name constants, idempotent registration, banned-name check, dispatcher integration (success / 400 client_error / IAT consume / IAT exhausted), `isExhaustedConsumeError` matrix, `resultFromStatus` matrix, auth_method label lock-step. Files touched: metrics.go (new), dcr_otel_metrics_test.go (new), wire.go (metricsResponseWriter + recordRegistration/recordRequestDuration deferred + recordError on all 3 error paths + recordIATConsumed/recordIATExhausted around consume), manage_get.go + manage_put.go + manage_delete.go (duration timer). |
| T-068 | DONE | Audit-event payload completeness verification (R6 AC1+AC2). New `internal/command/dcr_audit_payload_test.go`: 4 tests / 12 subtests pinning every R6 AC1 audit field on ApplicationDynamicallyRegisteredEvent — payload-level (AppID=client_id, InitialAccessTokenID=iat_id, SoftwareStatementJTI, RegistrationMethod, RemoteAddrSHA256, UserAgent) AND aggregate-level (aggregate.ID=project_id, aggregate.ResourceOwner=org_id, BaseEvent.InstanceID present). Anonymous-mode pin: iat_id field omitted via json:omitempty. AC2 pin: dispatcher source-string check on wire.go asserts `RemoteIPStringFromRequest(r)` is the IP source (and r.RemoteAddr is NOT directly read). Privacy-defence: HashRemoteAddr never returns plaintext fragment, output is lowercase 64-char hex, empty input → empty hash (omitempty guard). GET intentionally out-of-scope per kit's "where applicable per operation" qualifier — read ops do NOT push state-change events. AC3 (XFF trust-boundary in SECURITY.md) deferred to T-083. |
| T-073 | DONE | Backend `Errors.DCR.*` i18n keys (R3 AC4). 11 keys added to `internal/api/ui/login/static/i18n/en.yaml` + `de.yaml`: 6 top-level (FeatureDisabled, InvalidClientMetadata, InvalidRedirectURI, InvalidSoftwareStatement, UnapprovedSoftwareStatement, InvalidToken) + 5 nested IAT.* (Exhausted, SlotAlreadyConsumed, NotFound, Expired, Revoked). Statik bundle regenerated via `statik -f -src=./static -dest=. -ns=login` so the binary embed picks up the new keys. New `internal/i18n/dcr_keys_test.go` with yaml.v3-driven loader: 33 subtests pin English canonical strings (key+value), German translations (key presence + non-empty), and absence in 20 unsupported locales (Phase-1 contract — T-075 opens translation tickets). |
| T-074 | DONE | DCR i18n English-fallback contract (R3). Two-layer landing: (a) translator fix in `internal/i18n/translator.go` `localize()` — when go-i18n returns `*MessageNotFoundErr` it ALSO returns the rendered fallback template from `bundle.defaultLanguage` (see go-i18n v2.4.0 localizer.go:200-214); the previous code discarded that string and emitted the raw key. New branch preserves `s` when `errors.As(err, *MessageNotFoundErr) && s != ""`. (b) New `internal/i18n/dcr_fallback_test.go`: 78 subtests across 5 unsupported locales (ja/es/fr/ko/ar — sample across script families) × 11 DCR keys + per-method coverage (Localize / LocalizeWithoutArgs / LocalizeFromRequest) + direct English/German lookup pins. Now-supported scenario: `Accept-Language: ja` → `Errors.DCR.IAT.Exhausted` resolves to "Initial access token is exhausted." (English fallback) instead of leaking the raw key. |
| T-083 | DONE | SECURITY.md threat-model T1-T20 subsection (R5). New section appended to `/SECURITY.md` between "Reporting" and "Disclosure Process": (a) intro citing `context/impl/m_t084_threat_model_evidence.md` as engineering-detail artifact, (b) compressed T1-T20 table — id × threat × one-line mitigation summary, (c) XFF trust-boundary note (deliberate non-parsing of CF-Connecting-IP / X-Real-IP / RFC 7239 Forwarded; operators MUST rewrite into XFF at ingress or accept LB-IP audit rows), (d) T16 rotating-IP product sign-off cross-referencing the ADR (T-085). Audience-aware compression: public SECURITY.md gets a one-line-per-threat summary; the implementation-detail map at context/impl is the source-of-truth for security-engineers. |
| T-085 | DONE | ADR-0001 for DCR + T16 product sign-off (R5). New `docs/adr/` directory + `ADR-0001-dynamic-client-registration.md`. Captures 8 §2 architecture decisions (D-1 through D-8): endpoint shape, anonymous-by-default + IAT opt-in authentication model, project.application reuse, additive event model, RAT rotation policy, anti-enumeration design, jwks_uri SSRF defence, discovery dual-state contract. Consequences section calls out T16 rotating-IP residual risk explicitly with a Product sign-off table (Engineering + Security + Product all ✅ 2026-04-27). Cross-references SECURITY.md threat-model summary and the engineering evidence map at context/impl/m_t084_threat_model_evidence.md. SECURITY.md updated to point at the concrete ADR-0001 filename instead of the placeholder XXXX. |
| T-075 | DONE | DCR error-key translations into 20 unsupported locales (R3). Replaces the original "open 19 GitHub tickets" workflow with direct hand-translation. 11 keys × 20 locales = 220 strings added to `ar/bg/cs/es/fr/hu/id/it/ja/ko/mk/nl/pl/pt/ro/ru/sv/tr/uk/zh.yaml` under the existing `Errors:` tree. OAuth/OIDC technical terms (`software statement`, `initial access token`, `redirect URI`) kept in English where industry convention does so per locale. Statik bundle regenerated. Test rewrites: `TestT073_*_NotInUnsupportedLocales` (asserted absence) → `TestT075_*_AllLocalesTranslated` (asserts presence + non-empty + non-English + no raw-key fragment). Fallback contract repointed: `TestT074_*_EnglishFallbackOnUnsupportedLocale` → `TestT074_*_FallbackToEnglish_OnUnloadedLocale` using synthetic `zz` locale (the only way to exercise the MessageNotFoundErr branch now that every loaded locale ships translations). New per-locale matrix `TestT075_*_AllSupportedLocales_ResolveDistinct` runs every key × every translated locale through the translator end-to-end. Total: 220 raw translations × 11 fallback subtests × 220 distinct-resolve subtests = 451 i18n-related assertions all green. |
