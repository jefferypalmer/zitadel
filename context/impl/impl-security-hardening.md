---
created: "2026-04-24T00:00:00Z"
last_edited: "2026-04-27T14:00:00Z"
---
# Implementation Tracking: Security Hardening

Build site: context/plans/build-site.md

| Task | Status | Notes |
|------|--------|-------|
| T-062 | DONE | Log-redaction tests bundled with T-061 (`internal/api/oidc/dcr/redact_test.go` — 13 subtests). The kit's literal-named integration test files (`dcr_log_redaction_test.go` + `dcr_grpc_iat_logging_redaction_test.go`) are subsumed: M0 audit (T-006) + T-063 audit confirmed no production log-emission path exists for them to capture today. Should a future change introduce body-logging, those integration tests are added alongside the new code path; until then unit-level coverage is the structural guard. |
| T-061 | DONE | Defensive log-redaction utility per R3 AC3. New `internal/api/oidc/dcr/redact.go` with `RedactSecrets(string) string` covering 6 patterns: `zdiat_<id>.<random>` (greedy through `.` per R5 amendment), `zdrat_<random>`, JSON `client_secret`/`software_statement`/`registration_access_token`, and case-insensitive `Authorization: Bearer`. Half-redaction unsafe explicitly pinned via `TestRedactSecrets_HalfRedactionUnsafe` — neither id nor random portion survives. M0 + T-063 audits confirmed bodies aren't logged today; this utility is the wrapper R3 AC3 calls for. Existing dcr log sites emit operator-correlation IDs only (no secret strings); godoc contract requires future log sites to wrap via RedactSecrets. Build P, Tests P. |
| T-064 | DONE | Cross-tenant IAT abuse rejection (R6 T11 evidence). 3 subtests appended to `internal/api/oidc/dcr/auth_test.go`: (a) `CrossInstanceIATAbuse_Rejected` — IAT-bound-to-instA on req-landing-instB → invalid_token + dummy-Verify; (b) `CrossOrgIATAbuse_RegistrationContextBoundToIATOrg` — RegistrationContext.OrgID/ProjectID come from IAT row, not caller (downstream T-040 RegisterClient persists under that org regardless); (c) `CrossInstanceIAT_WrongInstanceCtx_DummyVerifyTimingMatch` — cross-instance branch MUST run Verify against dummy hash NOT stored hash, even if random would match (T12 cross-cut). Build P, Tests P. |
| T-060 | DONE | IAT projection-lag retry success rate (R5 AC4 / R7 / T18). New `internal/command/dcr_iat_projection_lag_test.go` — 1000-trial Monte Carlo with custom `laggyPusher` (eventstore.Pusher implementation) returning `ThrowAlreadyExists` at lagProb=0.35 per attempt. Each collision bumps a per-trial atomic.Int64 counter so the retry loop's next lookup observes the freshly committed slot. 3-retry max; expected success 1−0.35³≈95.7% asserted at the 95% kit floor. Observed run: 962/1000 (96.2%). Skipped under -short. lagProb=0.35 chosen as the threshold pinning point — a regression that weakens the retry loop below the binomial-formula expectation fails the test. |
| T-058 | DONE | Timing-side-channel real-Passwap pin (R4 / T12). New `internal/api/oidc/dcr/dcr_timing_side_channel_test.go` — 50-iteration timing test through a REAL bcrypt-cost-4 `passwap.Swapper` exercising `VerifyRAT` directly. Two branches: (a) unknown client_id with any RAT (NotFound→dummy Verify), (b) known client_id with WRONG RAT (real Verify against stored hash). Asserts ratio in [0.5, 2.0] — both branches MUST run real bcrypt-cost-4 verify exactly once. Observed ratio: 1.000. Mirrors `TestResolveIAT_F101_RealPasswapTimingEquivalence` methodology. Skipped under -short. Catches F-101-shape regressions where dummy hash uses a different algorithm than the configured Swapper (returns ErrNoVerifier in microseconds → ratio≪1 → fails). |
| T-063 | DONE | `internal/logstore/` IAT-leak audit. Audit artifact: `context/impl/m_t063_logstore_iat_audit.md` documents methodology + findings + cross-refs. Conclusion: NO leak surface — both record types (`AccessLog`, `ExecutionLog`) carry no request bodies; existing `AccessLog.Normalize()` at `record/access.go:83-90` already redacts `authorization`+`grpcgateway-authorization`+`cookie`+`grpcgateway-cookie`+response `set-cookie`. ExecutionLog action-author Message surface is out-of-scope (T-061 wrapper covers). New regression subtests in `internal/logstore/record/access_test.go`: (1) IAT-shaped Bearer `zdiat_<id>.<random>` redacted; (2) RAT-shaped Bearer `zdrat_...` redacted. Two literal-token shapes pin the redaction surface so a future regression that disables Authorization redaction or removes the header from the redact list surfaces here in addition to T-061's regex-wrapper coverage. R3 audit ✓; R6 T15 partially covered (full when T-061+T-062 land). Build P, Tests P. |
| T-065 | DONE | T17 unit test in dcr/handler_test.go pinning registration_endpoint dual-state contract from the dcr-package perspective. New `TestT065_RegistrationEndpoint_T17_DualState` (2 sub-tests): (1) disabled-state JSON marshal asserts `omitempty` drops the key entirely (no literal `"null"`); (2) enabled-state asserts the URL parses as scheme=https + correct host + path=HandlerPrefix, and JSON contains the field non-null. Constant assertion `HandlerPrefix == "/oidc/v1/register"` pins the discovery emission's path component so a rename surfaces a test failure here in addition to the byte-identical assertion in `dcr_discovery_test.go::TestDiscoveryConfig_RegistrationEndpoint_NeverNullInJSON` (T-029) and the cross-doc test `TestDiscoveryAndAsMetadata_R3_BothOmitRegistrationWhenDisabled` (T-047). T17 root cause documented inline: Claude Code's Zod parser rejects `null` on optional URL fields. Files: `internal/api/oidc/dcr/handler_test.go` (+test, +`net/url`+`strings` imports). Build P, Tests P. |
| T-059 | DONE | Hash-rotation cross-cut. R5 AC1 (RFC 7592 verify uses two-return form) + R5 AC2 (`.rehashed` event emission) + R5 AC3 (M4 affirmative decision: silent-rehash IS in scope, NOT deferred). Decision artifact: `context/impl/m4-hash-rotation-decision-t059.md` documents implementation evidence cross-referencing T-051's surface (`internal/api/oidc/dcr/manage.go:305` two-return Verify call + `RATVerifier` interface signature + `cmd/start/start.go:755` production wiring of `commands.SecretHasher()`). New test `TestT059_HashRotation_RealPasswap_TwoReturnForm` at `internal/api/oidc/dcr/manage_test.go` exercises the full two-return path through a REAL `passwap.NewSwapper(argon2id, bcrypt.Verifier)` rotation: stores a bcrypt-encoded RAT, calls VerifyRAT, asserts (a) verify succeeds, (b) Rehasher receives an argon2id-encoded hash distinct from original bcrypt encoding, (c) ManageContext.Rehashed=true. Distinct from `TestVerifyRAT_SilentRehash` (fake-verifier unit test) — T-059 catches regressions in Swapper-rotation wiring that the fake-verifier path masks. New `realRotationVerifier` adapter type. R5 AC4 (`dcr_iat_projection_lag_test.go` ≥95% retry success) cross-cut to T-060. Build P, Tests P. |
| T-003 | DONE | CORS reuse inspection. Decision artifact: `context/impl/m0-cors-reuse-t003.md`. DCR handlers will wrap in existing `middleware.CORSInterceptor`; no new config tree. `rs/cors` semantics under `AllowCredentials:true` + `AllowOriginFunc` satisfy R1 "never `*` + credentials". |
| T-006 | DONE | M0 log-redaction posture survey. Decision artifact: `context/impl/m0-log-redaction-survey-t006.md`. HTTP + gRPC middleware log NO bodies today; AccessLog already redacts Authorization/cookie headers; bodies not captured. T-061 still required for defensive redaction wrappers (protects against future debug-level logging). |
| T-015 | DONE | SSRF-guarded JWKS fetcher at `internal/api/oidc/dcr/jwks_fetcher.go`. CIDR deny-list via netip.Prefix, DNS resolved exactly once per hop with pinned dialer (DNS-rebind defense), 3-hop redirect cap with per-hop SSRF re-validation, 1 MiB body cap, configurable HTTPTimeout, AllowLoopbackInDev dev override. Public seams `resolve` / `dial` enable test injection without mocking net/http. |
| T-016 | DONE | 24 jwks_fetcher subtests covering full deny matrix (RFC 1918 / loopback / link-local incl. 169.254.169.254 metadata / IPv6 ULA + link-local + loopback), parseDeniedRanges dev-override, oversized body, exact-at-limit body, redirect chains (3 OK / 4 refused), redirect trap to private IP, DNS-rebind one-resolve guarantee, literal-IP rejection, scheme guard. dcr_ssrf_test.go integration test deferred to land with T-031/T-057. |
| T-084 | DONE | T1-T20 threat-model evidence map at `context/impl/m_t084_threat_model_evidence.md`. One row per threat → mitigation type → source paths → test evidence → build-site task. T9 (XSS) DOCS-MITIGATED until frontend tier (T-070, blocked on T-069). T16 (rotating-IP) docs-only by design — product sign-off lives in T-085 ADR. T19 (eventstore burst) deferred to runtime observability via zitadel.dcr.* metrics dashboards. T-083 (SECURITY.md) and T-085 (ADR) consume this artifact verbatim. |
