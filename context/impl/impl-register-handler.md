---
created: "2026-04-26T00:00:00Z"
last_edited: "2026-04-27T01:30:00Z"
---
# Implementation Tracking: DCR Register Handler

Build site: context/plans/build-site.md

| Task | Status | Notes |
|------|--------|-------|
| T-008 | DONE | Dual-gate handler mount. `internal/api/oidc/dcr/handler.go` stub: 403 `feature_disabled` (RFC 7591 envelope, no-store/no-cache headers, only the 2 RFC fields — no leakage) when runtime flag off, 200 placeholder body when both gates ON. `cmd/start/start.go` conditionally mounts `/oidc/v1/register` before `oidcPrefixes` so gorilla mux routes the more specific prefix. yaml off → mux 404. 4 subtests green. T-031 replaces the single HandlerFunc with method-aware mux routing. |
| T-044 | DONE (AC3 deferred to T-079) | TLS posture inspection (R10). AC1 (same TLS as /oidc/v1/userinfo) verified structurally — both mount on the same `apis` server in `cmd/start/start.go:693` and `:702`; `apis.RegisterHandlerOnPrefix` has no per-handler TLS override, so the entire API server's `config.TLS` / `config.ExternalSecure` posture is inherited. AC2 (no DCR-specific TLS knobs) pinned by new `TestDCRConfig_NoTLSKnobs_R10` in `internal/api/oidc/dcr_config_test.go` — reflection-based field scan over `DCRConfig` + `DCRJwksURIConfig` rejects any future field whose name contains TLS/Cert/HTTPS/Insecure/MTLS/Mtls. AC3 (deployment-guide TLS-termination note) deferred to T-079 (DCR MDX page) which owns the `cavekit-console-ui-docs-and-observability.md` R5 doc text. Inspection artifact: `context/impl/m_t044_tls_posture_inspection.md`. Test P. |
| T-034 | DONE (T-035 absorbed; F-100 hardened 2026-04-27) | RFC 7591 metadata clamp + validate. **2026-04-27 F-100 hardening:** the original hand-rolled `extractHost` parser was vulnerable to RFC 3986 userinfo bypass (`https://victim.example.com:443@evil.com/cb` matched `*.example.com`). Replaced with `net/url.Parse` + `u.Hostname()` per amended cavekit-register-handler.md R4. Userinfo-carrying URLs now hard-rejected as `invalid_redirect_uri` per OAuth 2.1 §4.1.2. 4 bypass shapes covered by `TestValidateAndClampMetadata_R4_UserinfoBypassRejected`. Original entry: | New `internal/api/oidc/dcr/metadata.go` with `RFC7591Metadata` struct (RFC 7591 §2 + OIDC Reg 1.0 §2 fields, pointer types where needed for absent-vs-empty distinction). validate.go skeleton replaced with `ValidateAndClampMetadata(cfg DCRConfigSubset, in *RFC7591Metadata, supportedIDTokenSigAlgs []string, softwareStatementEnabled bool) (*RFC7591Metadata, error)` enforcing all 13 R4 acceptance criteria: grant_types/response_types/auth_method/application_type intersection (auth_method special-cases `client_secret_jwt` rejection with policy reason), subject_type pairwise rejection, id_token_signed_response_alg vs supported list, request_object_* full rejection, software_statement+feature-off → unapproved_software_statement, MaxRedirectURIs count clamp, redirect URI host-pattern + `domain.GetOIDCV1Compliance` integration. **T-035 absorbed**: loopback HTTP (localhost / 127.0.0.1 / [::1]) accepted only for `application_type=native` per RFC 8252 §7.3, exempt from host-pattern check; web/UA loopback still rejected. `DCRConfigSubset` interface allows clamp to be reused by T-054 PUT. `ClampError` carries RFC 7591 envelope code + `DCR-Vt0XX` zerrors ID per T-032 convention. Returned struct is a copy — input never mutated (verified by test). 17 test groups / 30+ subtests P incl. happy-path, intersection, all 13 ACs individually, isLoopbackHTTP table, hostMatches table. |
| T-035 | DONE (absorbed into T-034) | Loopback HTTP redirect URI acceptance for `application_type=native` + `domain.GetOIDCV1Compliance` integration. Implementation in `internal/api/oidc/dcr/validate.go::CheckRedirectURIs` + `runOIDCV1Compliance` (T-034 commit 66f16cf99). Loopback hosts (localhost / 127.0.0.1 / [::1] with arbitrary port) bypass the host-pattern check ONLY when application_type=native (RFC 8252 §7.3); web/UA loopback rejected. `domain.GetOIDCV1Compliance` invoked with the converted enum types via `mapApplicationType / mapGrantTypes / mapAuthMethod` helpers — non-compliant result mapped to `invalid_redirect_uri` envelope. Verified by `TestValidateAndClampMetadata_R4_NativeLoopbackHTTP` (3 subtests) + `TestValidateAndClampMetadata_R4_NativeLoopbackHTTP_NotAllowedForWeb` + `TestIsLoopbackHTTP` (11-row table) — all P. |
| T-039 | DONE | OIDCAppFromRFC7591Metadata mapping. New function in `internal/domain/application_oidc.go` accepting primitives (clientName, redirectURIs, grantTypes, responseTypes, tokenEndpointAuthMethod, applicationType, postLogoutRedirectURIs, backChannelLogoutURI) and returning `*domain.OIDCApp`. Performs only structural conversion — caller MUST clamp first via T-034. Sets `OIDCVersion=V1` (kit out-of-scope: OIDC v2). Internal helpers `rfc7591GrantTypes / ResponseTypes / AuthMethod / ApplicationType` map RFC 7591 vocab → domain enums. Unknown vocabulary → nil pointer (downstream tolerates). Bridge: `(*RFC7591Metadata).ToOIDCApp()` in `internal/api/oidc/dcr/metadata.go` translates the wire-format struct to the primitives signature. Layering kept clean — `internal/domain` does NOT import `internal/api/oidc/dcr`. **Pass-through fields:** new `(*RFC7591Metadata).BuildDCRMeta() map[string]any` extracts the 12 RFC 7591 §2 fields kit R6 specifies for the `dcr_meta` JSONB column (contacts, logo_uri, client_uri, policy_uri, tos_uri, software_id, software_version, default_max_age, require_auth_time, default_acr_values, initiate_login_uri, scope). Empty/nil fields are omitted so the persisted JSON stays small. Returns nil if all fields empty. T-040 RegisterClient command will combine `ToOIDCApp()` + `BuildDCRMeta()` for storage. 5 domain tests (HappyPath, ApplicationTypeMapping 4-row, AuthMethodMapping 4-row, UnknownVocabulary, OmitsBackchannelWhenEmpty) + 5 dcr tests (HappyPath, NilSafe×2, AllPassthroughFields, OmitsZeroValues, PartialFields) — all P. |
| T-038 | DONE | Anonymous-mode auth routing (R3 AC4-AC5). New `internal/api/oidc/dcr/auth.go` with `RegistrationContext{InstanceID,OrgID,ProjectID,IATID}` + `AnonymousConfig` interface + `AuthMode`/`ClassifyAuthMode` (Bearer-vs-anonymous header split, lowercase/uppercase/whitespace tolerant, Basic/Digest treated as anonymous per RFC 7591 §2 only defining Bearer) + `ResolveAnonymous(ctx, cfg) (*RegistrationContext, error)`. InstanceID sourced from `authz.GetInstance(ctx).InstanceID()` (single source of truth across all handlers — no DCR-specific host parsing). Org/project from `cfg.DefaultOrgID()` / `DefaultProjectID()`. iat_id="" sentinel preserved verbatim per AC5. Defensive runtime guard: returns `feature_disabled` ClampError when defaults are empty (T-009 catches at startup; this is the runtime defence-in-depth for hot-reload). When `RequireInitialAccessToken=true` returns `invalid_token` ClampError so handler maps to 401 + WWW-Authenticate (R3 AC1). 7 ClassifyAuthMode subtests + happy-path + IAT-required + 5-row defensive-defaults test. |
| T-037 | DONE (post-DE-001 resolution) | IAT-mode auth routing (R3 AC1-AC3, AC6). DE-001 resolved 2026-04-26 via `/ck:revise --trace` — option 1 (embed IAT ID in plaintext) selected after security review. Kit amended in commit ae2290c83 (cavekit-iat.md R3/R4/R5 + cavekit-security-hardening.md R3). Implementation: `command/iat.go::GenerateIATPlaintextForID` (allocates ID before plaintext; new format `zdiat_<id>.<random>`), `command/iat.go::ParseIATPlaintext` (strings.Cut first-dot split + ID-alphabet [A-Za-z0-9_-]+), `query/initial_access_token.go::InitialAccessTokenByHash` + SQL + `token_hash` index DELETED, `dcr/auth.go::ResolveIAT(ctx, queries, verifier, parse, presented)` runs parse → byID → instance-binding check → VerifyIATPlaintext, with anti-enumeration dummy-Verify on parse-failure / not-found / wrong-instance branches per the R4 amendment AC. Returns RegistrationContext{InstanceID, OrgID=ResourceOwner, ProjectID, IATID=row.ID}. Slot-consume deferred to T-040 RegisterClient command for atomicity. 5 ResolveIAT subtests + 4 ParseIATPlaintext subtests (4 reject + 3 accept) + the existing T-038 anonymous suite — all P. |
| T-041 | DONE | appProjection reducers for the DCR application events (R6 / T-040 follow-on). Files: internal/query/projection/app.go (+3 nullable columns on apps7_oidc_configs Init: `registration_access_token_hash` TEXT, `registration_access_token_expires_at` TIMESTAMPTZ, `dcr_meta` JSONB; +2 reducer registrations + bodies — `reduceApplicationDynamicallyRegistered` UPDATEs dcr_meta JSONB only when payload carries non-empty DCRMeta else no-op; `reduceApplicationRegistrationAccessTokenSet` UPDATEs hash + optionally expires_at — NULL expires_at = never-expires per cavekit-manage-handler.md R2); internal/query/projection/app_test.go (+4 new reducer subtests); internal/repository/project/dynamic_client_registration.go (+`DCRMeta map[string]any` json:omitempty field on `ApplicationDynamicallyRegisteredEvent` payload — additive, older events unmarshal with DCRMeta=nil; constructor signature gains the param at the end); internal/command/dynamic_client_registration.go (RegisterClient now passes `in.DCRMeta` into the new event constructor — closes the gap where T-040 collected DCRMeta in the input struct but never persisted it). **Schema-migration safety:** all 3 new columns nullable so existing non-DCR-registered apps stay untouched; the apps7 → apps7-with-DCR migration is purely additive (no data backfill needed). **Audit-only fields stay in eventstore:** initial_access_token_id / software_statement_jti / registration_method / client_name_unclamped / remote_addr_sha256 / user_agent are NOT projected — they are audit-trail material, queried via the eventstore directly. Build P, Tests P (4 new app projection subtests; full app_test.go suite still green; command + repository + dcr packages all clean). |
| T-042 | DONE | 201 response shape (R7). Files: internal/api/oidc/dcr/response.go (new — `RegistrationResponse` wire struct + `RegistrationOutput` writer-input + `WriteRegistrationResponse` + `buildRegistrationResponse` pure helper + `clientSecretExpiresAtFor` + `buildRegistrationClientURI`); internal/api/oidc/dcr/response_test.go (new — 12 subtests). All R7 ACs pinned: status=201; Content-Type=`application/json;charset=UTF-8`; Cache-Control=no-store; Pragma=no-cache; client_secret omitted via `omitempty` when absent (auth_method=none/private_key_jwt); client_secret_expires_at=0 sentinel ALWAYS present (RFC 7591 §3.2.1 MUST-emit); client_id_issued_at unix-seconds; registration_access_token plaintext echoed once; registration_client_uri = `<issuer>/oidc/v1/register/<client_id>` via `op.IssuerFromContext` (matches T-029 discovery `registration_endpoint` URL — single source of truth via `dcr.HandlerPrefix`); trailing-slash trim on issuer; full clamped metadata echoed (every R4 field that survived). **No-mutation pin** — buildRegistrationResponse never aliases the Clamped struct (asserted by TestBuildRegistrationResponse_DoesNotMutateInput so a future async OTel span reading the same struct cannot see corrupted state). Issuer-empty fallback yields a relative URL (`/oidc/v1/register/<id>`) — production wiring rides through `op.IssuerFromContext` middleware so the fallback never fires. **Out of scope for T-042**: handler dispatch (POST decode → auth → clamp → command → respond) is T-033's domain; T-042 ships only the writer so T-033 + T-057 can compose. Build P, Tests P (12 R7 subtests + full DCR package). |
| T-036 | DONE | Client-secret behavior matrix per auth method (R5). Files: internal/api/oidc/dcr/validate.go (+AC3 — `private_key_jwt` REQUIRES `jwks_uri`; missing → `invalid_client_metadata` envelope with field `jwks_uri` + zerrors `DCR-Vt0R5`); internal/api/oidc/dcr/validate_test.go (+TestValidateAndClampMetadata_R5_AuthMethodMatrix 6-row + TestValidateAndClampMetadata_R5_PrivateKeyJWT_RejectsBlankJwksURI). **Coverage of R5 ACs**: AC1 (none → no secret) is enforced at command layer by T-040 RegisterClient via `domain.SetNewClientSecretIfNeeded` (which only generates when auth_method=basic/post), pinned by `TestRegisterClient_HappyPath_AnonymousMode_NoSecret`. AC2 (basic/post → secret + Passwap hash + plaintext returned once) pinned by `TestRegisterClient_BasicAuth_EmitsClientSecret` — `mockHashedSecret` returns "hashed-client-secret" and the test asserts `res.ClientSecret` echoes it. AC3 (private_key_jwt → jwks_uri REQUIRED) added in this task. AC4 (client_secret_jwt → 400 invalid_client_metadata) was already enforced by T-034 special-case at validate.go:124. **PKCE S256 enforcement** (R5 AC1 second clause) is a /authorize-time concern — the registration validator stores auth_method=none and lets the existing OIDC `domain.GetOIDCV1Compliance` path (invoked at clamp time via T-035 absorbed into T-034) reject a none+web combination at registration; runtime PKCE enforcement is owned by the OIDC stack, not DCR. Native loopback redirect AC for `none` covered by T-035 (absorbed into T-034). SSRF guard for jwks_uri fetch (T-015) is wired in at the handler dispatch site (future T-033/T-042 work) — the validator only enforces presence, not reachability. Build P, Tests P. |
| T-040 | DONE | RegisterClient command + 2 new project events. Files: `internal/repository/project/dynamic_client_registration.go` (new — `ApplicationDynamicallyRegisteredEvent` + `ApplicationRegistrationAccessTokenSetEvent` + mappers), `internal/repository/project/eventstore.go` (mappers registered), `internal/command/dynamic_client_registration.go` (new — `RegisterClient` + `HashRemoteAddr` + `generateRATPlaintext` + `IsRATPlaintext`), `internal/command/dynamic_client_registration_test.go` (new — 12 subtests). Pushes the R6 4-event sequence in order on the project aggregate: ApplicationAddedEvent → OIDCConfigAddedEvent → ApplicationDynamicallyRegisteredEvent → ApplicationRegistrationAccessTokenSetEvent. **Reuses existing OIDCApplicationWriteModel** via the same OIDCConfigAddedEvent constructor as `addOIDCApplicationWithID`; deliberately skips the FilterToQueryReducer dedupe call because the appID is freshly minted by `c.idGenerator.Next()` (snowflake — collision-free by construction) so the duplicate-app check is moot. Project existence is the caller's contract (anonymous mode validates DefaultProjectID at startup per T-009; IAT mode binds via the IAT row's project_id per T-037). **RAT format:** `zdrat_` + base64url(48 random bytes) — distinct prefix from IAT (`zdiat_`) so the cavekit-security-hardening.md R3 redaction regex `zd(iat\|rat)_[^\s"',]+` can target both. Passwap-encoded hash via `c.secretHasher.Hash`; plaintext NEVER touches the eventstore (assertion-tested). **remote_addr_sha256:** `HashRemoteAddr(remoteIPString)` SHA-256s the raw `RemoteIPStringFromRequest` result; empty input → empty (audit field omitted via `json:omitempty`); plaintext IP NEVER persisted. Returns `RegisterClientResult{ClientID, ClientSecret, RATPlaintext, RATExpiresAt, ClientIDIssuedAt}` — `ClientSecret` empty unless auth_method ∈ {basic, post}; `RATExpiresAt` zero-valued when `RATLifetime=0` (RFC 7591 §3.2.1 sentinel). Out of scope for T-040: handler integration (T-042 wires the response shape; the handler dispatcher work that calls RegisterClient is part of T-033/T-042 frontier). 12 subtests all P (HashRemoteAddr 5 cases; generateRATPlaintext 2; IsRATPlaintext 6; RegisterClient happy-path-anonymous + basic-auth-IAT + RATLifetime + 6-row input-validation table). |
| T-031 | DONE | DCR mux router skeleton — promotes the T-008 single HandlerFunc to a gorilla `*mux.Router` multiplexing POST/GET/PUT/DELETE on `/oidc/v1/register{/*}`. Routes: POST `/` → register stub (T-033), GET/PUT/DELETE `/{client_id}` → 501 stubs (T-053/T-054/T-056). `featureGateMiddleware` wraps the entire router so the runtime feature flag fires BEFORE method routing — important so 403 responses don't leak the surface map (probing attacker can't distinguish "wrong method" from "wrong path" when gate is off). 405 handler returns RFC 7591 envelope with `error: "invalid_request"` for known paths called with wrong method. StrictSlash so empty + "/" both route to POST /. 7 method-routing subtests + 6 gate-overrides-routing cases pin the matrix. |
