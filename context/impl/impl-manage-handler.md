---
created: "2026-04-24T00:00:00Z"
last_edited: "2026-04-27T10:30:00Z"
---
# Implementation Tracking: RFC 7592 Management Handler

Build site: context/plans/build-site.md

| Task | Status | Notes |
|------|--------|-------|
| T-056 | DONE | RFC 7592 DELETE + path-(a) `RevokeApplicationTokens` per T-007 M4 decision. **New commands** (`internal/command/dynamic_client_registration.go`): (1) `RevokeApplicationTokens(ctx, clientID)` — walks the eventstore via new `applicationTokenRevocations` write model (filters `oidcsession` aggregate type with EventData{clientID} on AddedType + per-token events on a second branch, reduces per-aggregate state in-memory, emits one revocation event per active session — uses RefreshTokenRevoked alone when both flags set since the session model treats refresh-revoke as superset of access-revoke); (2) `DeleteRegisteredClient(ctx, projectID, orgID, appID, clientID)` — composes RevokeApplicationTokens + ApplicationRemovedEvent push (separate Pushes by aggregate boundary; revocation-first ordering chosen so a step-2 failure leaves "revoked-but-app-remains" rather than the inverse). Bypasses `Commands.RemoveApplication`'s checkPermissionDeleteApp because RFC 7592 RAT-only requests have no user CtxData — RAT verified upstream by VerifyRAT. **New dcr surface** (`manage_delete.go` + `manage.go` + `wire.go`): DeleteFn/DeleteRequest seam on ManageDeps + deleteClientHandler emits 204 + chooseDELETEHandler stub-vs-real selector mirroring choosePUTHandler. Uses writeAuthError so a future ClampError carrying invalid_token (e.g. projection-lag NotFound) gets WWW-Authenticate per R8. **Tests:** `manage_delete_test.go` (+6 subtests: stub fall-back when Delete=nil; Success_204 + ManageContext routing pin; DispatchError_Mapped500 with R8 fingerprint guard; ClampErrorPropagates with WWW-Authenticate; NoBearer_401_DoesNotCallDelete; defensive 500 on missing ManageContext). **Integration test** (`dcr_delete_revokes_tokens_test.go` //go:build integration): 7-step flow — IAT mint → DCR register → PKCE token exchange → pre-DELETE userinfo sanity → DELETE+RAT → post-DELETE userinfo expects 401 (Eventually with 10s timeout for projection lag) → refresh-token grant expects 4xx. **start.go** Delete closure wiring. All R6 ACs pinned: AC1 (204) ✓ — TestDELETE_Success_204 + integration test. AC2 (revocation events emitted before/with RemoveApplication) ✓ — DeleteRegisteredClient sequence. AC3 path-(a) (integration test asserts revoke + introspect/refresh rejection) ✓ — dcr_delete_revokes_tokens_test.go. AC5 (decision recorded in M4 worker report) ✓ — context/impl/m4-token-revocation-decision-t007.md. Build P, vet -tags integration P, Tests P (dcr + command + repository/project). |
| T-055 | DONE | RFC 7592 PUT-side RAT rotation. Layered on top of T-054 PUT pipeline. **New event:** `ApplicationRegistrationAccessTokenRotatedType` = `project.application.registration_access_token.rotated` — distinct from `.set` (initial issuance) and `.rehashed` (silent algorithm rotation, plaintext unchanged). Carries hashedToken + expiresAt; old-RAT invalidation falls out of projection reducer overwriting hash column (next VerifyRAT against old plaintext fails on Passwap.Verify — no explicit revocation event needed). **Files (modified):** `internal/repository/project/dynamic_client_registration.go` (+`ApplicationRegistrationAccessTokenRotatedType` const + `ApplicationRegistrationAccessTokenRotatedEvent` struct + `NewApplicationRegistrationAccessTokenRotatedEvent` factory + mapper); `internal/repository/project/eventstore.go` (+RegisterFilterEventMapper); `internal/query/projection/app.go` (+reducer registration + `reduceApplicationRegistrationAccessTokenRotated` — updates hash always, expires_at only when non-zero so a rotation that doesn't specify a lifetime cannot silently extend an existing finite-lifetime RAT); `internal/query/projection/app_test.go` (+2 reducer subtests: no-expiry vs with-expiry SQL emission); `internal/command/dynamic_client_registration.go` (+`RATLifetime time.Duration` on UpdateRegisteredClientInput; +`RATPlaintext`/`RATExpiresAt` on UpdateRegisteredClientResult; +rotation block in UpdateRegisteredClient — generateRATPlaintext + secretHasher.Hash + push rotated event atomically with config-changed and secret-changed events); `internal/api/oidc/dcr/manage.go` (+`RATPlaintext`/`RATExpiresAt` on UpdateResult); `internal/api/oidc/dcr/manage_put.go` (+`RegistrationAccessToken string \`json:"registration_access_token"\`` on ManageUpdateResponse — no omitempty so the field is mandatory on every successful PUT 200; surfaced via buildManageUpdateResponse); `internal/api/oidc/dcr/manage_put_test.go` (+3 R5 AC7-9 subtests: AlwaysEmitsNewRAT pinning rotation field present + raw-JSON check / PresentEvenWithoutAuthMethodTransition pinning rotation independence from auth-method matrix / NotEmittedOnError pinning 4xx envelopes don't carry the rotated token); `cmd/start/start.go` (+RATLifetime: config.OIDC.DCR.RegistrationAccessToken.Lifetime + RATPlaintext/RATExpiresAt round-trip on Update closure). All R5 T-055 ACs pinned: AC7 (new RAT generated + hashed via Passwap + persisted via rotated event + old immediately invalid) ✓ — projection reducer SQL pinned + handler emits non-empty plaintext. AC8 (PUT response 200 with new RAT in body, same shape as registration response) ✓ — `TestPUT_RATRotation_AlwaysEmitsNewRAT` raw-JSON inspection. AC9 (retried PUT with old RAT → 401) ✓ — old hash overwritten in projection; idempotency caveat already in T-054 docs cross-ref. RAT rotation event audit ✓ — distinct wire-type from `.set`/`.rehashed`. Build P, Tests P (dcr + command + repository/project + projection + cmd/start). |
| T-054 | DONE | RFC 7592 PUT body re-clamp + auth-method transition matrix. **Files (new):** `internal/api/oidc/dcr/manage_put.go` (+`ManageUpdateResponse` wire struct + `putClientHandler` + `buildManageUpdateResponse` pure helper); `internal/api/oidc/dcr/manage_put_test.go` (+13 subtests: stub fall-back when Update nil; happy path success; AC3 none→basic emits client_secret incl. raw-JSON check; AC4 basic→none omits client_secret via raw-JSON; AC5 private_key_jwt missing jwks_uri→400; AC5 private_key_jwt happy-path with jwks_uri; AC6 client_secret_jwt rejected; disallowed grant_type→400; 415 non-JSON; 400 malformed JSON; 413 body cap; 500 mapping for non-ClampError; ClampError pass-through; defensive 500 on missing ManageContext; Validate refusing Config=nil when Update set). **Files (modified):** `internal/command/dynamic_client_registration.go` (+`UpdateRegisteredClientInput`/`UpdateRegisteredClientResult`/`UpdateRegisteredClient` command — re-projects via `getOIDCAppWriteModel`, pushes `OIDCConfigChangedEvent` via `existingOIDC.NewChangedEvent`, atomic auth-method transition: `none→client_secret_*` mints new secret via `c.newHashedSecret` and pushes `OIDCConfigSecretChangedEvent`; `client_secret_*→none` pushes `OIDCConfigSecretChangedEvent` with empty hash to clear stored column); `internal/api/oidc/dcr/manage.go` (+`Update UpdateFn` on ManageDeps + `Config DCRConfigSubset` + `SupportedSigAlgs []string` + `SoftwareStatementEnabled bool` + `MaxBodyBytes int64` + `UpdateRequest`/`UpdateResult` shapes + Validate guards Config!=nil when Update set); `internal/api/oidc/dcr/wire.go` (+`choosePUTHandler` selector — falls back to `putClientStub` 501 when Update nil so deployments mid-rollout produce uniform 501 envelope; otherwise mounts `putClientHandler`); `cmd/start/start.go` (+`Update` closure on Manage block bridging `dcr.UpdateRequest` → `command.UpdateRegisteredClientInput`; reuses `oidc.DCRClampAdapter` + `oidc.SupportedSigningAlgs()` + `config.OIDC.DCR.SoftwareStatement.Enabled` + `config.OIDC.DCR.MaxRequestBodyBytes` so PUT and POST share clamp config). All R5 T-054 ACs pinned: AC1 (full re-clamp via shared `validate.go` — same R4 rules POST register uses) ✓. AC2 (disallowed→400 invalid_client_metadata) ✓ — `TestPUT_DisallowedGrantType_400` + `TestPUT_AuthMethodMatrix_ClientSecretJWTRejected`. AC3 (none→client_secret_* issues new secret in body) ✓ — `TestPUT_AuthMethodMatrix_NoneToBasic_EmitsClientSecret` raw-JSON check. AC4 (client_secret_*→none clears + omits client_secret) ✓ — `TestPUT_AuthMethodMatrix_BasicToNone_OmitsClientSecret` raw-JSON inspection. AC5 (*→private_key_jwt requires jwks_uri) ✓ — `TestPUT_AuthMethodMatrix_PrivateKeyJWT_RequiresJwksURI` (rejected at clamp before Update closure runs) + happy-path counterpart. AC6 (* → client_secret_jwt rejected) ✓ — clamp catches before command. RAT rotation (AC7-9) deliberately NOT in scope — that's T-055 (it will compose new RAT into ManageUpdateResponse). Build P, Tests P (dcr + command + cmd/start all green). |
| T-007 | DONE | M4 decision gate: DELETE token-revocation path (a) `RevokeApplicationTokens` SELECTED over path (b) docs-only. Artifact: `context/impl/m4-token-revocation-decision-t007.md`. Evidence: `RevokeOIDCSessionToken` (oidc_session.go:266) is per-token, no bulk equivalent; no `SessionsByClientID` query helper exists today; `RemoveApplication` (project_application.go:121) does not revoke tokens. Path (a) requires new query + command in T-056; primitives (`AccessTokenRevokedEvent`, `RefreshTokenRevokedEvent`, `oidc_session` projection with clientID) exist. Path (b) rejected — contradicts RFC 7592 §4 REQUIRES language. |
| T-052 | DONE | Anti-enumeration on unknown client_id pinned across GET/PUT/DELETE. **Files (new):** `internal/api/oidc/dcr/manage_antienum_test.go` (+11 subtests in 3 groups: 9-row matrix `{GET,PUT,DELETE} × {unknown_client_id, wrong_RAT, missing_header} → 401 + WWW-Authenticate` + dummy-Verify call recorded against AntiEnumDummyHash on the unknown-client_id branch; `TestManage_AntiEnum_DummyHashSourceContract` pinning ManageDeps.Validate refusal of empty AntiEnumDummyHash — F-101 lesson; `TestManage_AntiEnum_NoCacheOn401` pinning Cache-Control: no-store + Pragma: no-cache on anti-enum 401s — T14 cross-ref so a CDN can't permanently pin a transient 401). R3 ACs: AC1 (401 not 404 across all three verbs) ✓ — `verbs := [GET, PUT, DELETE]` table parameterizes the same dispatch chain. AC2 (dummy-hash Verify call against AntiEnumDummyHash on unknown branch) ✓ — `vr.calls[0].encoded == "$argon2id$dummy"` structural assertion. AC3 (WWW-Authenticate on every 401 path including missing-header + wrong-RAT + unknown-client_id) ✓ — three test groups verify all three paths. R3 implementation already lived in T-051's VerifyRAT NotFound branch; this task is the cross-handler pinning that prevents a future per-verb refactor from regressing only one verb without a test failure. T-058's actual nanosecond-level timing-side-channel test is the downstream consumer (kit cross-ref documented in test prelude). Build P, Tests P (full dcr package green). |
| T-053 | DONE | RFC 7592 GET /oidc/v1/register/{client_id} body. **Files (new):** `internal/query/dcr_metadata_by_client_id.sql` + `internal/query/dcr_rat.go` (+`DCRGetMetadata` struct + `DCRMetadataByClientID` query helper — same instance-scoped + `registration_access_token_hash IS NOT NULL` defence as DCRRATLookupByClientID); `internal/api/oidc/dcr/manage_get.go` (+`ManageReadResponse` + private `dcrMetaPassthrough` decoder + `getClientHandler` + `buildManageReadResponse` pure helper); `internal/api/oidc/dcr/manage_get_test.go` (+7 subtests: success+full body shape with dcr_meta unpacking / R4 AC3 client_secret + RAT omission via raw-JSON inspection / client_secret_expires_at sentinel-0 presence / registration_client_uri shared with POST via `buildRegistrationClientURI` / 500 on metadata lookup error post-Verify / malformed dcr_meta still serves 200 with empty pass-through / no-ManageContext defensive 500). **Files (modified):** `internal/domain/application_oidc.go` (+`RFC7591GrantTypeStrings`/`RFC7591ResponseTypeStrings`/`RFC7591AuthMethodString`/`RFC7591ApplicationTypeString` — inverse of existing `rfc7591*` mappers, used by the cmd/start adapter to convert int enums back to RFC 7591 wire strings); `internal/api/oidc/dcr/manage.go` (+`DCRMetadataByClientID` on `ManageQueries` interface + `ManageMetaRow` struct mirror of query.DCRGetMetadata + `ClientSecretExpiresIn` on `ManageDeps` for future per-secret expiry derivation); `internal/api/oidc/dcr/manage_test.go` (+`metaRow`/`metaErr` fields on fakeManageQueries + `DCRMetadataByClientID` stub method); `internal/api/oidc/dcr/wire.go` (`manageRoutes` now wraps GET in `getClientHandler(*deps.Manage)` instead of getClientStub when Manage configured — PUT/DELETE remain stubs until T-054/T-056); `cmd/start/start.go` (+`ClientSecretExpiresIn: config.OIDC.DCR.ClientSecretExpiresIn` on ManageDeps + `dcrManageQueriesAdapter.DCRMetadataByClientID` translating `query.DCRGetMetadata` → `dcr.ManageMetaRow` via the new domain.RFC7591*Strings helpers). All R4 ACs pinned: AC1 200 success ✓. AC2 body fields complete + `client_secret_expires_at=0` sentinel always present ✓. AC3 `client_secret` + `registration_access_token` omitted (raw-JSON inspection) ✓. AC4 `registration_client_uri` shared with POST register via the existing `buildRegistrationClientURI` helper ✓. AC5 Content-Type + Cache-Control + Pragma headers ✓. Build P, Tests P (dcr + query + projection + domain + cmd/start). |
| T-051 | DONE | RAT verify w/ Passwap two-return + silent rehash + expiry + 401. **Files (new):** `internal/repository/project/dynamic_client_registration.go` (+`ApplicationRegistrationAccessTokenRehashedType` event + struct + `NewApplicationRegistrationAccessTokenRehashedEvent` + mapper); `internal/repository/project/eventstore.go` (+`RegisterFilterEventMapper` for rehashed); `internal/query/projection/app.go` (+reducer registration + `reduceApplicationRegistrationAccessTokenRehashed` — updates ONLY hash column, leaves expires_at untouched per R2 "lifetime not changed" rule); `internal/query/projection/app_test.go` (+1 reducer subtest pinning the hash-only UPDATE statement); `internal/query/dcr_rat_lookup_by_client_id.sql` + `internal/query/dcr_rat.go` (+`DCRRATLookup` struct + `DCRRATLookupByClientID` query helper — instance-scoped via WHERE; `registration_access_token_hash IS NOT NULL` filter excludes legacy projects-app surface so the manage path is opaque to non-DCR apps); `internal/api/oidc/dcr/manage.go` (+`ManageQueries`/`ManageRATRow`/`RATVerifier`/`Rehasher`/`ManageContext`/`ManageDeps`/`VerifyRAT`/`manageVerifyDispatch`/`contextWithManage`/`ManageFromContext`); `internal/api/oidc/dcr/manage_test.go` (+11 subtests: success / NotFound→401+dummy-Verify / WrongRAT→401-no-extra-dummy / SilentRehash→event pushed w/ correct project+org+app routing / RehashFailureDoesNotFailVerification / Expired→401 / FutureExpiryAccepted / 4-mutation Validate matrix / dispatch happy-path delegates ManageContext via ctx / dispatch missing-Bearer short-circuits / dispatch NotFound→401+WWW-Authenticate). **Files (modified):** `internal/command/dynamic_client_registration.go` (+`RehashRegistrationAccessToken` command — explicit (projectID, orgID, appID) args because RFC 7592 RAT-only requests carry no user CtxData); `internal/api/oidc/dcr/wire.go` (+`Manage *ManageDeps` field on `RegistrationDeps` — optional; Validate cascades; +`manageRoutes` selector — falls back to T-050 bearer-presence-only gate when Manage nil so mid-rollout deployments still 401 instead of 500); `cmd/start/start.go` (+`Manage` block on dcrDeps with adapter — `commands.SecretHasher()` directly satisfies `RATVerifier` since `*crypto.Hasher` embeds `*passwap.Swapper.Verify`; reuses `dcrDummyHash` for AntiEnumDummyHash; +`dcrManageQueriesAdapter` translating `*query.DCRRATLookup` → `*dcr.ManageRATRow`). All R2 ACs pinned: AC1 two-return Verify call ✓ (TestVerifyRAT_Success/WrongRAT). AC2 silent-rehash event emission ✓ (TestVerifyRAT_SilentRehash; verifies (projectID, orgID, appID, hash) routing). AC3 expiry check ✓ (TestVerifyRAT_Expired/FutureExpiryAccepted). AC4 401 + WWW-Authenticate on failure ✓ (TestManageVerifyDispatch_NotFound_401/MissingBearer). Anti-enum dummy-Verify on NotFound is the prerequisite for T-052 (timing-equivalence proof at the cross-handler structural level lives in T-058). Build P, Tests P (dcr + repository/project + query + projection + command + cmd/start). |
| T-050 | DONE | RFC 7592 mount + missing-Bearer 401 gate. Files: `internal/api/oidc/dcr/handler.go` (+`manageBearerGate` middleware wrapping `getClientStub`/`putClientStub`/`deleteClientStub` — uses shared `ClassifyAuthMode`; emits 401 + `WWW-Authenticate: Bearer error="invalid_token"` + RFC 7591 envelope when no Bearer present); `wire.go` (same wrap on `NewHandler` mount); `handler_test.go` (added `bearer` field on `TestHandler_MethodRouting` cases — GET/PUT/DELETE 501 stubs now exercised with Bearer; new `TestHandler_ManageBearerGate` 6-row matrix pinning missing/empty/non-Bearer→401; new `TestHandler_ManageGate_FeatureOff` pinning feature-gate-runs-before-bearer-gate ordering — feature-off → 403 even with Bearer). AC1 (401 missing Bearer) ✓. AC2 (yaml-off → 404) ✓ via existing T-008/T-031 unmount path. AC3 (runtime-off → 403) ✓ via existing `featureGateMiddleware` running before bearer gate; pinned by new `TestHandler_ManageGate_FeatureOff`. AC4 (shared mux precedence) ✓ — POST `/` and `/{client_id}` GET/PUT/DELETE registered on the same `mux.Router`. RAT verification (R2) deferred to T-051; anti-enum dummy-Verify (R3) deferred to T-052. |
| T-032 | DONE | Shared `errors.go` + `validate.go` skeleton in `internal/api/oidc/dcr/`. errors.go exports ErrorEnvelope (RFC 7591 §3.2.2) + WriteError (sets `Content-Type: application/json;charset=UTF-8` + Cache-Control no-store + Pragma no-cache + envelope JSON) + RFC 7591 error-code constants (ErrCodeInvalidRedirectURI / ErrCodeInvalidClientMetadata / ErrCodeInvalidSoftwareStatement / ErrCodeUnapprovedSoftwareStatement / ErrCodeFeatureDisabled / ErrCodeInvalidToken / ErrCodeInvalidRequest / ErrCodeNotImplemented). zerrors-ID convention `DCR-<5 alphanumeric>` documented in trailing comment for T-033/T-034/T-036/T-040/T-054 to adopt. validate.go is doc-only skeleton announcing the public API (ValidateAndClampMetadata + ApplyDefaultsRFC7591 + CheckRedirectURIs) that T-033/T-034/T-054 will fill — keeping the file present reserves the import path. handler.go refactored to consume `WriteError` + `ErrCodeInvalidRequest` / `ErrCodeFeatureDisabled` / `ErrCodeNotImplemented`; existing 17 subtests still green (`go test ./internal/api/oidc/dcr/...` 0.008s). |
