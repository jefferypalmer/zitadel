---
created: "2026-04-26T00:00:00Z"
last_edited: "2026-04-26T20:00:00Z"
---
# Implementation Tracking: Initial Access Token (IAT) Domain

Build site: context/plans/build-site.md

| Task | Status | Notes |
|------|--------|-------|
| T-011 | DONE | Three IAT events on the `project` aggregate: `project.initial_access_token.added`, `.consumed`, `.revoked`. Files: `internal/repository/project/initial_access_token.go` + mappers registered in `eventstore.go`. Consumed event declares per-slot UniqueConstraint `iat_uses:<id>:<use_index>` when finite (`finite=true` constructor flag); nil when MaxUses=0. `finite` is NOT serialized (reconstructed from projection at consume time by T-017). 9 subtests green incl. wire-type pin, payload, omitempty optional fields, finite-vs-unbounded constraint matrix, distinct-IAT-distinct-slot collision space. |
| T-017 | DONE | Race-safe ConsumeInitialAccessToken at `internal/command/iat.go`. IATLookup closure seam + IATSnapshot struct decouple from query.Queries. 3-retry loop: re-fetches projection on every iteration, rejects on revoked/expired/exhausted before push, picks slot=UsesConsumed, pushes ConsumedEvent(finite=MaxUses>0). On AlreadyExists from eventstore (per-slot UniqueConstraint collision) → loop; non-AlreadyExists errors bubble up. After 3 collisions → Errors.DCR.IAT.Exhausted. Also adds RevokeInitialAccessToken (idempotent at projection). 9 subtests green (HappyPath / Revoked / Expired / ExhaustedPrePush / Unlimited / RetriesAfterAlreadyExists / Recovers_OnSecondAttempt / NonAlreadyExistsErrorBubblesUp / LookupErrorBubbles). |
| T-018 | DONE | Concurrency test at `internal/command/iat_concurrency_test.go` via fakeIATPusher implementing eventstore.Pusher in ~30 lines (mutex-guarded constraint set + projection state). Three scenarios: (a) MaxUses=3 / 10 racers → 3 succeed, 7 exhausted; (b) MaxUses=4 / 4 racers → all 4 succeed via retries; (c) MaxUses=4 / 5 racers → 4 succeed, 1 exhausted. **Clean under `go test -race -count=1000` (~3.9s)** — zero flakes. Note: kit phrasing for scenario (c) said "max_uses=5 with 5 racers → 4+1" but that's arithmetically inconsistent; corrected to MaxUses+1 racers as same shape. |
| T-019 | DONE | `projections.initial_access_tokens` projection at `internal/query/projection/initial_access_token.go`. Schema: composite PK (instance_id, id), token_hash, expires_at NULL, max_uses BIGINT (kit said INT but framework only offers Int64), uses_consumed BIGINT default 0, consumed_slots SMALLINT[] default '{}' (kit said INT[] but framework's int-array is ColumnTypeEnumArray = SMALLINT[]; SMALLINT comfortably holds any reasonable max_uses), allowed_grant_types TEXT[] NULL, allowed_redirect_uri_patterns TEXT[] NULL, revoked BOOL default false, plus standard timestamps + sequence. Indices: (instance_id, project_id) + (token_hash). 5 reducers: Added INSERT, Consumed (increment uses_consumed + array_append slot), Revoked (revoked=true), ProjectRemoved (DELETE rows for project), InstanceRemoved (helper). Registered in projection.go. 6 reducer subtests pin SQL shape + arg types. |
| T-020 | DONE | Query helpers at `internal/query/initial_access_token.go`: InitialAccessTokenByID(ctx, id, resourceOwner) and InitialAccessTokenByHash(ctx, tokenHash). Both instance-scoped via authz.GetInstance(ctx); ByID also has optional resource_owner scope (empty = admin path skips org check). Cross-instance/cross-org → ThrowNotFound (Errors.DCR.IAT.NotFound) — structurally safe via WHERE clause, not app-level filtering. SQL embedded via go:embed; row_to_json wrap so QueryJSONObject decodes directly into InitialAccessToken struct. 6 subtests via sqlmock. |
| T-021 | DONE | IAT plaintext format + Passwap hashing at `internal/command/iat.go`. Format: `zdiat_` + base64url(48 random bytes) = 70 chars / 384 bits entropy. M1 prefix-collision check passed against at_/rt_/sess_ (verified via grep at implementation). GenerateIATPlaintext + IsIATPlaintext (cheap structural check) + CreateInitialAccessToken command (generates plaintext, hashes via existing secretHasher, pushes Added event with hash + metadata, returns plaintext + iat_id) + VerifyIATPlaintext (Passwap.Verify wrapper, anti-enumeration: same Errors.DCR.IAT.InvalidToken on mismatch and missing). 4 tests green incl. format / prefix discriminator / collision guard. ListInitialAccessToken non-leakage of plaintext satisfied structurally by projection schema (T-019). |
| T-022 | DONE | Three RPCs added to AdminService in `proto/zitadel/admin.proto`: CreateInitialAccessToken / ListInitialAccessTokens / RevokeInitialAccessToken with google.api.http (POST /admin/v1/initial_access_tokens / .../_search / .../{iat_id}/_revoke), zitadel.v1.auth_option (iam.write / iam.read / iam.write), and openapiv2_operation with new "Initial Access Tokens" swagger tag. Messages: CreateInitialAccessTokenRequest (project_id + lifetime + max_uses + allowed_grant_types + allowed_redirect_uri_patterns + description), CreateInitialAccessTokenResponse (iat_id + token + expires_at + details), ListInitialAccessTokensRequest (ListQuery + project_id), ListInitialAccessTokensResponse (ListDetails + InitialAccessTokenView[]), InitialAccessTokenView (id + project_id + description + max_uses + uses_consumed + revoked + timestamps + lists), RevokeInitialAccessTokenRequest (iat_id + project_id), RevokeInitialAccessTokenResponse (details). No new proto file; generated stubs build clean via `nx run @zitadel/api:generate-stubs` (gitignored under pkg/grpc/admin/). |
| T-023 | DONE | Admin gRPC handler bodies at `internal/api/grpc/admin/iat.go`. CreateInitialAccessToken forwards to c.command.CreateInitialAccessToken (T-021). ListInitialAccessTokens calls new q.SearchInitialAccessTokens (added at `internal/query/initial_access_token.go` + `initial_access_tokens_search.sql` with json_agg + COALESCE for empty result). RevokeInitialAccessToken forwards to c.command.RevokeInitialAccessToken (T-017). iatViewToPb conversion never includes plaintext (structurally safe — projection only has hash). |
| T-024 | DONE | Dual-gate via iatDualGate(ctx) at top of all 3 IAT handlers. yaml off (Server.dcrYAMLEnabled=false) → codes.Unimplemented (gRPC-equivalent "not registered"). yaml on + runtime flag off → codes.FailedPrecondition + "Errors.DCR.FeatureDisabled" message key (matches HTTP handler's 403 envelope). yaml on + flag on → proceed. Server gains dcrYAMLEnabled bool field; CreateServer takes new flag; cmd/start/start.go passes config.OIDC.DCR.Enabled. 7 subtests pinning gate matrix. |
| T-025 | DONE | Serialization-characteristic note in `internal/command/iat.go` godoc on both ConsumeInitialAccessToken and CreateInitialAccessToken: "Concurrent consumption of IATs from the same project is serialized by eventstore aggregate locking; use multiple projects for parallelism." (verbatim per kit R7 AC). |
